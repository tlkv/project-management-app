// eslint-disable-next-line no-useless-escape
export const passRegExp = /^[a-zA-Z0-9!@#$&()\\-`.+,/\"]{8,30}$/; // /^[A-Za-z0-9]\w{7,}$/;
export const userRegExp = /^[A-Za-z0-9]{4,20}$/;
export const message = '8-30 letters (eng) or numbers or ! @ # $ & ( ) - ‘ . / + ,';
